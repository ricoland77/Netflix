import Images from "./Images";

const Section = (props) => {
  // console.log(props.title);
  // console.log(props.tabImg);
  return (
    <>
      <h2>{props.title}</h2>
      <img src="" alt="" />
    </>
  );
};

export default Section;
